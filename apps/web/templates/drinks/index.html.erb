<% if drinks.any? %>
  <% drinks.each do |drink| %>
    <div class="drink">
      <h2><%= drink.name %></h2>
      <p><%= drink.country %></p>
    </div>
  <% end %>
<% else %>
  <p class="placeholder">There are no drinks yet.</p>
<% end %>
